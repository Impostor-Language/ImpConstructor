// public editor //
    Main Class

      public float="terminal"
      public float="string"
         GenerateCommand{ string + float
         CommandName="AllServers"
         GenerateCommand{ string + float
         CommandName="CreateServer"
 //Tokens//
       CommandLink { string + float
       CommandSource="AllServers"
    string + { == float.host
    impostor.server == {  string.id
         AllServer.host
    LinkKey= { + 0000000 { 
      generateKey { = _this.key
        ServerExtensions.{ Id
 
     CommandLink { string + float
     CommandSource="CreateServer"
         string.impostor
      GenerateServer.( = AmongTasksManager.exe
      Server.impostor( = { string + float
           data. { { = id  
                }
    createServer.id( = Tasks.float + string
      float.host( = createServer
          ipLocal = { host.Id }
     
    
  
  
