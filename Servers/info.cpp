#include <iostream>
#include <game>
#include <server>
   namespace.bat
      string ( = < 0 )
    string( = < 1 ( AmongUs.exe
     open ( = x
     open ( server.keys
        file(system.id
      open ( = x, z
          servers.host( =  3 { Among us }
          server.host( = 1 { server.ip }
      open(name.json)
         host.server ( = { 2 server.ip }
      open.namespace.file == { file
      jsonscript ( f, source.info
          server.isHost("CreateGame")
   
    
 
    
